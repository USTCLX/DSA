import mergeTwoLists from "./mergeTwoLists";
describe("Test of mergeTwoLists", () => {
  test("should ", () => {
    expect(mergeTwoLists([1, 2, 3], [1, 2, 4])).toEqual([1, 1, 2, 2, 3, 4]);
  });
});
